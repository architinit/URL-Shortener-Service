services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=dev-secret-key
      - DATABASE_URL=sqlite:////app/data/shortener.db
